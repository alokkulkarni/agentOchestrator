# =============================================================================
# AGENT ORCHESTRATOR - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your values
# The .env file is automatically loaded by both servers (normal and Docker mode)
#
# Usage:
#   1. Copy: cp .env.example .env
#   2. Edit .env with your API keys and settings
#   3. Start servers: python3 -m model_gateway.server
#   4. Or Docker: docker-compose up -d
#
# =============================================================================

# =============================================================================
# AI Provider API Keys
# =============================================================================

# Anthropic Claude API Key (Required for Claude models)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Tavily Search API Key (Optional - for web search agent)
# Get free key from: https://tavily.com
# 1000 searches/month on free tier
TAVILY_API_KEY=tvly-your-key-here

# OpenWeatherMap API Key (Optional - for real weather data)
# Get free key from: https://openweathermap.org/api
# 1000 calls/day on free tier
# If not set, weather agent will use mock data
OPENWEATHER_API_KEY=your-openweather-key-here

# AWS Credentials (Optional - for Bedrock models)
# Only needed if using AWS Bedrock provider
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_REGION=us-east-1
AWS_PROFILE=

# =============================================================================
# Model Gateway Configuration
# =============================================================================

# Server Settings
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8585
GATEWAY_RELOAD=false
GATEWAY_LOG_LEVEL=INFO
GATEWAY_LOG_REQUESTS=true

# Authentication (set to true to enable)
GATEWAY_REQUIRE_AUTH=false
GATEWAY_API_KEY=

# Provider Credentials (uses ANTHROPIC_API_KEY from above)
GATEWAY_ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}

# =============================================================================
# Agent Orchestrator Configuration
# =============================================================================

# API Server Settings
ORCHESTRATOR_API_HOST=0.0.0.0
ORCHESTRATOR_API_PORT=8001
ORCHESTRATOR_API_RELOAD=false
ORCHESTRATOR_API_LOG_LEVEL=info

# Authentication (set to true to enable)
ORCHESTRATOR_REQUIRE_AUTH=false

# Configuration Files
ORCHESTRATOR_CONFIG=config/orchestrator.yaml
AGENTS_CONFIG=config/agents.yaml
RULES_CONFIG=config/rules.yaml

# =============================================================================
# Observability - Distributed Tracing (OpenTelemetry)
# =============================================================================

# OTLP Endpoint (set if using Jaeger/other OTLP collector)
# Leave empty to disable distributed tracing
OTEL_EXPORTER_OTLP_ENDPOINT=
# OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4317  # For Docker mode

# Console exporter (set to true for debugging)
OTEL_CONSOLE=false

# =============================================================================
# Logging Configuration
# =============================================================================

# Docker logging driver
LOG_DRIVER=json-file
LOG_MAX_SIZE=10m
LOG_MAX_FILE=3

# =============================================================================
# Rate Limiting
# =============================================================================

# Enable/disable rate limiting
RATE_LIMIT_ENABLED=true

# Default rate limits (per minute)
RATE_LIMIT_DEFAULT=100/minute

# =============================================================================
# MCP Server URLs (Optional - for MCP-based agents)
# =============================================================================

# MCP Calculator Server (Optional)
# Only needed if you want to run the MCP calculator example
MCP_CALCULATOR_URL=http://localhost:8080

# MCP Weather Server (Optional)
MCP_WEATHER_URL=stdio

# =============================================================================
# Development Settings
# =============================================================================

# Set to true for auto-reload on code changes
# Only use in development!
DEV_RELOAD=false

# =============================================================================
# Notes
# =============================================================================
#
# Required for basic operation:
#   - ANTHROPIC_API_KEY (for Claude models)
#
# Optional but recommended:
#   - TAVILY_API_KEY (for web search)
#   - OPENWEATHER_API_KEY (for real weather data)
#
# Optional for AWS:
#   - AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY (for Bedrock)
#
# For Docker mode:
#   - All variables are automatically loaded from .env
#   - No need to export them manually
#
# For normal mode:
#   - Variables are loaded automatically when servers start
#   - You can also export them: export ANTHROPIC_API_KEY=...
#
# =============================================================================
